# Starter

Scaffolded by `keel init --new`. A Kotlin + Spring Boot API and a TypeScript frontend wired for the keel loop: one contract, tagged tests, coverage and Playwright.

Next: `keel ladder`, then `/keel:feature <your first idea>`.
